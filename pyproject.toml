[tool.ruff]
target-version = "py313"
indent-width = 2
extend-exclude = [
  ".venv",
]
src = [
  "app",
  "examples/demo-backend",
  "tests",
]

[tool.ruff.format]
indent-style = "space"

[tool.bandit]
exclude_dirs = [
  ".venv",
]
