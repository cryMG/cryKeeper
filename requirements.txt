## Runtime dependencies for cryKeeper.
## Pinned exactly for reproducible production builds.
## For local development and CI quality tools, install requirements-dev.txt instead.

Flask==3.1.3
gunicorn==26.0.0
redis==8.0.0
altcha==2.0.0
prometheus-client==0.25.0
