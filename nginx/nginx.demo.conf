worker_processes 1;

events {
    worker_connections 1024;
}

http {
    map $host $gatekeeper_path_prefix {
        default /gatekeeper;
        cap.localhost /cap-check;
        full.localhost /full-check;
        dummy.localhost /dummy-check;
        hcaptcha.localhost /hcaptcha-check;
        altcha.localhost /altcha-check;
    }

    upstream gatekeeper_app {
        server gatekeeper:5000;
    }

    upstream demo_backend_app {
        server demo-backend:5001;
    }

    server {
        listen 80;
        server_name _;
        return 308 https://$host:8443$request_uri;
    }

    server {
        listen 443 ssl;
        server_name full.localhost;
        absolute_redirect off;
        ssl_certificate /etc/nginx/certs/demo.crt;
        ssl_certificate_key /etc/nginx/certs/demo.key;
        ssl_session_cache shared:demo_tls:10m;
        ssl_session_timeout 10m;

        location = /_gatekeeper_check {
            internal;
            proxy_pass http://gatekeeper_app$gatekeeper_path_prefix/check;
            proxy_pass_request_body off;
            proxy_set_header Content-Length "";
            proxy_set_header Cookie $http_cookie;
            proxy_set_header Host $http_host;
            proxy_set_header User-Agent $http_user_agent;
            proxy_set_header X-Forwarded-For $remote_addr;
            proxy_set_header X-Forwarded-Proto $scheme;
            proxy_set_header X-Original-Method $request_method;
            proxy_set_header X-Original-URI $request_uri;
        }

        location @gatekeeper_challenge {
            proxy_pass http://gatekeeper_app$auth_redirect;
            proxy_set_header Cookie $http_cookie;
            proxy_set_header Host $http_host;
            proxy_set_header X-Forwarded-Host $http_host;
            proxy_set_header X-Forwarded-Proto $scheme;
            proxy_set_header X-Forwarded-For $remote_addr;
        }

        location ~ ^/full-check(/|$) {
            proxy_pass http://gatekeeper_app;
            proxy_set_header Host $http_host;
            proxy_set_header X-Forwarded-Host $http_host;
            proxy_set_header X-Forwarded-Proto $scheme;
            proxy_set_header X-Forwarded-For $remote_addr;
        }

        location / {
            auth_request /_gatekeeper_check;
            auth_request_set $auth_redirect $upstream_http_x_auth_redirect;
            error_page 401 =403 @gatekeeper_challenge;

            proxy_pass http://demo_backend_app;
            proxy_set_header Host $http_host;
            proxy_set_header X-Forwarded-Host $http_host;
            proxy_set_header X-Forwarded-Proto $scheme;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        }
    }

    server {
        listen 443 ssl;
        server_name _;
        absolute_redirect off;
        ssl_certificate /etc/nginx/certs/demo.crt;
        ssl_certificate_key /etc/nginx/certs/demo.key;
        ssl_session_cache shared:demo_tls:10m;
        ssl_session_timeout 10m;

        location = /_gatekeeper_check {
            internal;
            proxy_pass http://gatekeeper_app$gatekeeper_path_prefix/check;
            proxy_pass_request_body off;
            proxy_set_header Content-Length "";
            proxy_set_header Cookie $http_cookie;
            proxy_set_header Host $http_host;
            proxy_set_header User-Agent $http_user_agent;
            proxy_set_header X-Forwarded-For $remote_addr;
            proxy_set_header X-Forwarded-Proto $scheme;
            proxy_set_header X-Original-Method $request_method;
            proxy_set_header X-Original-URI $request_uri;
        }

        location @gatekeeper_challenge {
            proxy_pass http://gatekeeper_app$auth_redirect;
            proxy_set_header Cookie $http_cookie;
            proxy_set_header Host $http_host;
            proxy_set_header X-Forwarded-Host $http_host;
            proxy_set_header X-Forwarded-Proto $scheme;
            proxy_set_header X-Forwarded-For $remote_addr;
        }

        location ^~ /cap/ {
            proxy_pass http://cap:3000/;
            proxy_set_header Host $http_host;
            proxy_set_header X-Forwarded-Host $http_host;
            proxy_set_header X-Forwarded-Proto $scheme;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        }

        location /protected/ {
            auth_request /_gatekeeper_check;
            auth_request_set $auth_redirect $upstream_http_x_auth_redirect;
            error_page 401 =403 @gatekeeper_challenge;

            proxy_pass http://demo_backend_app;
            proxy_set_header Host $http_host;
            proxy_set_header X-Forwarded-Host $http_host;
            proxy_set_header X-Forwarded-Proto $scheme;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        }

        location = /gatekeeper {
            proxy_pass http://gatekeeper_app;
            proxy_set_header Host $http_host;
            proxy_set_header X-Forwarded-Host $http_host;
            proxy_set_header X-Forwarded-Proto $scheme;
            proxy_set_header X-Forwarded-For $remote_addr;
        }

        location ~ ^/(gatekeeper|cap-check|full-check|dummy-check|hcaptcha-check|altcha-check)(/|$) {
            proxy_pass http://gatekeeper_app;
            proxy_set_header Host $http_host;
            proxy_set_header X-Forwarded-Host $http_host;
            proxy_set_header X-Forwarded-Proto $scheme;
            proxy_set_header X-Forwarded-For $remote_addr;
        }

        location / {
            proxy_pass http://demo_backend_app;
            proxy_set_header Host $http_host;
            proxy_set_header X-Forwarded-Host $http_host;
            proxy_set_header X-Forwarded-Proto $scheme;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        }
    }
}
